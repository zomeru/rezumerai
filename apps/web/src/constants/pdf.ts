/**
 * US Letter page width in pixels at 96 DPI (standard web DPI).
 * Calculated as 8.5 inches × 96 DPI = 816px.
 * Used for PDF generation and resume preview sizing.
 */
export const LETTER_WIDTH_PX = 816; // 8.5 * 96

/**
 * US Letter page height in pixels at 96 DPI (standard web DPI).
 * Calculated as 11 inches × 96 DPI = 1056px.
 * Used for PDF generation and resume preview sizing.
 */
export const LETTER_HEIGHT_PX = 1056; // 11 * 96

{
  "name": "rezumerai",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "predev": "turbo run build --filter=@rezumerai/database",
    "build": "turbo build",
    "build:web": "turbo run build --filter=!@rezumerai/api",
    "build:api": "turbo run build --filter=!web --filter=!@rezumerai/ui",
    "build:production": "NODE_ENV=production turbo build",
    "build:packages": "turbo build --filter=./packages/*",
    "dev": "turbo dev",
    "start": "turbo start",
    "check": "turbo check",
    "check-types": "turbo check-types",
    "lint-staged": "lint-staged",
    "code:check": "bun run check && bun run check-types",
    "code:verify": "bun run code:check && bun run test:project && bun run build",
    "test": "turbo test",
    "test:watch": "turbo test:watch",
    "test:ui": "turbo test:ui",
    "test:coverage": "turbo test:coverage",
    "test:project": "vitest --run",
    "test:watch:project": "vitest --watch",
    "test:ui:project": "vitest --ui",
    "test:coverage:project": "vitest run --coverage",
    "changeset": "changeset",
    "publish:packages": "changeset publish",
    "version:packages": "turbo build && changeset version",
    "add-shadcn-component": "turbo run add-shadcn-component -- --",
    "db:setup": "cd packages/database && bun run db:push && bun run db:generate",
    "db:studio": "cd packages/database && bun run db:studio",
    "db:reset": "cd packages/database && bun run with-env prisma migrate reset --force",
    "docker:db": "docker compose up -d database redis",
    "docker:build": "./scripts/docker-compose-build.sh",
    "docker:build:standalone": "./scripts/docker-build.sh",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "install-modules": "bun install && turbo run build --filter=./packages/*",
    "update-modules": "bun update --latest && bun update --interactive --recursive",
    "clean": "turbo clean && rimraf node_modules .turbo tsconfig.tsbuildinfo .playwright-mcp",
    "clean:install": "turbo clean && rimraf node_modules .turbo bun.lock && bun install && turbo run build --filter=./packages/*",
    "outdated": "bun outdated",
    "security:audit": "bun audit --production",
    "security:check": "bunx audit-ci --config .audit-ci.json",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.15",
    "@changesets/changelog-github": "^0.5.2",
    "@changesets/cli": "^2.29.8",
    "@vitest/coverage-v8": "^4.0.18",
    "husky": "^9.1.7",
    "lint-staged": "16.2.7",
    "postcss": "^8.5.6",
    "rimraf": "^6.1.2",
    "turbo": "^2.8.8",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "overrides": {
    "body-parser": "^2.2.1",
    "hono": "^4.10.3",
    "valibot": "^1.2.0",
    "lodash": "4.17.23"
  },
  "trustedDependencies": [
    "@prisma/client",
    "@swc/core",
    "esbuild",
    "prisma",
    "sharp"
  ],
  "lint-staged": {
    "**/*.{js,jsx,ts,tsx,json,md,yaml,yml}": [
      "biome check --write --no-errors-on-unmatched"
    ]
  },
  "packageManager": "bun@1.3.8",
  "dependencies": {
    "zustand": "^5.0.11"
  }
}
